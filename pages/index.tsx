import type { NextPage } from 'next';
import { Seo } from '../components';

const Home: NextPage = () => {
   return (
      <div>
         <Seo title='Home' />
      </div>
   );
};

export default Home;
